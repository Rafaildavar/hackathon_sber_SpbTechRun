# 🚀 Гайд по запуску проекта "Городской помощник"

Этот гайд поможет вам быстро запустить проект на вашем компьютере.

## 📋 Требования

Перед началом убедитесь, что у вас установлено:

- **Python 3.10+** (проверить: `python --version` или `python3 --version`)
- **Docker** и **Docker Compose** (для базы данных PostgreSQL)
- **Git** (для клонирования репозитория)

## 🔧 Пошаговая инструкция

### Шаг 1: Клонирование репозитория

Если у вас еще нет кода проекта, склонируйте репозиторий:

```bash
git clone <URL_вашего_репозитория>
cd hackathon_sber_SpbTechRun
```

### Шаг 2: Создание виртуального окружения (рекомендуется)

Создайте виртуальное окружение для изоляции зависимостей:

```bash
# Для Linux/Mac:
python3 -m venv venv
source venv/bin/activate

# Для Windows:
python -m venv venv
venv\Scripts\activate
```

После активации виртуального окружения в начале строки терминала появится `(venv)`.

### Шаг 3: Установка зависимостей

Установите все необходимые Python-пакеты:

```bash
pip install -r requirements.txt
```

Если возникают проблемы, попробуйте:
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4: Запуск базы данных PostgreSQL

Проект использует PostgreSQL в Docker. Запустите базу данных:

```bash
docker-compose up -d
```

Эта команда:
- Скачает образ PostgreSQL 15 (если его нет)
- Создаст контейнер с базой данных
- Запустит его в фоновом режиме

Проверить, что контейнер запущен:
```bash
docker ps
```

Вы должны увидеть контейнер `city_helper_postgres` со статусом `Up`.

**Параметры подключения к БД:**
- Хост: `localhost`
- Порт: `5433`
- База данных: `city_helper`
- Пользователь: `postgres`
- Пароль: `postgres`

### Шаг 5: Инициализация базы данных (опционально)

Таблицы создаются автоматически при первом запуске приложения, но вы можете создать их вручную:

```bash
python init_db.py
```

### Шаг 6: Запуск приложения

Запустите FastAPI приложение одним из способов:

**Способ 1: Через Python (с автоперезагрузкой)**
```bash
python app.py
```

**Способ 2: Через uvicorn напрямую (рекомендуется)**
```bash
uvicorn app:app --host 0.0.0.0 --port 5001 --reload
```

Флаг `--reload` включает автоматическую перезагрузку при изменении кода.

### Шаг 7: Проверка работы

После запуска вы увидите сообщение:
```
INFO:     Uvicorn running on http://0.0.0.0:5001 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
База данных подключена и таблицы созданы/проверены
INFO:     Application startup complete.
```

Откройте в браузере:

- 🌐 **Главная страница**: http://localhost:5001/
- 💬 **Страница чата**: http://localhost:5001/chat
- 👤 **Страница входа**: http://localhost:5001/auth/login
- 📝 **Страница регистрации**: http://localhost:5001/auth/register
- 📚 **API документация (Swagger)**: http://localhost:5001/docs
- 📖 **Альтернативная API документация (ReDoc)**: http://localhost:5001/redoc

## 🎯 Первый запуск

1. Откройте http://localhost:5001/
2. Нажмите "Регистрация" или перейдите на http://localhost:5001/auth/register
3. Заполните форму регистрации (логин, пароль, город, район)
4. После регистрации вы автоматически попадете на страницу чата
5. Начните общаться с ботом!

## 🛠️ Структура проекта

```
hackathon_sber_SpbTechRun/
├── app.py                 # Основной файл FastAPI приложения
├── database.py            # Модели базы данных (User, Chat, Message)
├── init_db.py            # Скрипт инициализации БД
├── requirements.txt      # Python зависимости
├── docker-compose.yml    # Конфигурация PostgreSQL
├── resources/
│   ├── static/          # Статические файлы (CSS, JS, изображения)
│   └── templates/       # HTML шаблоны
└── yazzh_api/           # API модули для работы с городскими данными
```

## 🗄️ База данных

Проект использует PostgreSQL с следующими таблицами:

- **users** - информация о пользователях
- **chats** - чаты пользователей
- **messages** - сообщения в чатах

Таблицы создаются автоматически при первом запуске приложения.

## 🔌 API эндпоинты

### Аутентификация
- `POST /api/register` - регистрация нового пользователя
- `POST /api/login` - вход пользователя
- `POST /api/logout` - выход пользователя
- `GET /api/check-auth` - проверка авторизации
- `GET /api/user` - получение данных пользователя
- `PUT /api/user` - обновление данных пользователя
- `POST /api/change-password` - изменение пароля

### Чаты
- `GET /api/chats` - получить список чатов пользователя
- `POST /api/chats` - создать новый чат
- `GET /api/chats/{chat_id}` - получить чат по ID
- `PUT /api/chats/{chat_id}` - обновить чат
- `DELETE /api/chats/{chat_id}` - удалить чат
- `GET /api/chats/{chat_id}/messages` - получить сообщения чата
- `POST /api/chats/{chat_id}/messages` - создать сообщение

## ⚠️ Решение проблем

### Проблема: "Connection refused" при подключении к БД

**Решение:**
1. Проверьте, что Docker запущен: `docker ps`
2. Если контейнер не запущен: `docker-compose up -d`
3. Проверьте логи: `docker-compose logs postgres`

### Проблема: "Module not found" при запуске

**Решение:**
1. Убедитесь, что виртуальное окружение активировано
2. Переустановите зависимости: `pip install -r requirements.txt`

### Проблема: Порт 5001 уже занят

**Решение:**
Измените порт в `app.py` или запустите с другим портом:
```bash
uvicorn app:app --host 0.0.0.0 --port 5002 --reload
```

### Проблема: Ошибки при создании таблиц

**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте подключение: `docker exec -it city_helper_postgres psql -U postgres -d city_helper`
3. Удалите и пересоздайте контейнер:
   ```bash
   docker-compose down -v
   docker-compose up -d
   ```

### Проблема: Cookies не работают (сессии не сохраняются)

**Решение:**
- Убедитесь, что используете `credentials: 'include'` в fetch запросах
- Проверьте, что CORS настроен правильно в `app.py`

## 🛑 Остановка приложения

Для остановки приложения:
1. Нажмите `Ctrl+C` в терминале, где запущен сервер
2. Для остановки базы данных: `docker-compose down`

Для полной остановки и удаления данных:
```bash
docker-compose down -v
```

## 🔄 Обновление кода

Если вы получили обновления из репозитория:

```bash
git pull origin main
pip install -r requirements.txt  # если добавились новые зависимости
```

Перезапустите приложение.

## 📝 Переменные окружения (опционально)

Вы можете настроить приложение через переменные окружения:

```bash
export DATABASE_URL="postgresql://postgres:postgres@localhost:5433/city_helper"
export SECRET_KEY="your-secret-key-here"
```

Или создайте файл `.env` (не коммитьте его в git!):
```
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/city_helper
SECRET_KEY=your-secret-key-here
```

## 🆘 Получение помощи

Если что-то не работает:
1. Проверьте логи приложения в терминале
2. Проверьте логи PostgreSQL: `docker-compose logs postgres`
3. Убедитесь, что все зависимости установлены
4. Проверьте, что порты не заняты другими приложениями

## ✅ Чеклист перед запуском

- [ ] Python 3.10+ установлен
- [ ] Docker и Docker Compose установлены
- [ ] Репозиторий склонирован
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] PostgreSQL запущен (`docker-compose up -d`)
- [ ] Приложение запущено (`python app.py` или `uvicorn app:app --reload`)
- [ ] Браузер открыт на http://localhost:5001/

---

**Удачи в разработке! 🚀**

